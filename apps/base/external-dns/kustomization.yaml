apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: external-dns-system
resources:
  - namespace.yaml
  - repository.yaml
  - secret.yaml
  - release.yaml
configMapGenerator:
  - name: external-dns-values
    files:
      - values.yaml
