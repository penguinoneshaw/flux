apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
configMapGenerator:
  - name: traefik-values
    namespace: traefik-system
    files:
      - values.yaml
resources:
  - repository.yaml
  - release.yaml
